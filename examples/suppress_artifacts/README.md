# Suppress artifacts

Planned example: run each conditioned model on an artifact-corrupted CT using
externally downloaded checkpoints. Metadata is an additional input for the
anatomy-metadata model. Commands will be added with the inference implementation.
