# External pretrained weights

Checkpoint files will be hosted outside GitHub. Download URLs and checksums are
not available yet. The release will require a shared autoencoder checkpoint, an
anatomy-conditioned diffusion checkpoint, and an anatomy-metadata-conditioned
checkpoint containing its metadata encoder.

Store local downloads under `checkpoints/` at the repository root. A versioned
registry will be added here once the release artifacts and their checksums exist.
