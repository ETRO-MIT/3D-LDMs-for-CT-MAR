# Inference configurations

Add the shared autoencoder and both conditioned model configurations during
migration. Preserve the checkpoint architecture, scheduler, latent scaling, and
metadata vocabulary. Use configurable checkpoint paths instead of cluster paths.
