# Synthetic generation configurations

The initial command uses explicit CLI options; run `ct-mar-generate --help`.
Each output JSON records the resolved simulation settings. No paper-validated
configuration preset has been added yet.
